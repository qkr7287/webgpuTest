# 3D 엔진 비교 종합 정리

> **테스트 기반**: Babylon.js (WebGL) vs Three.js (WebGL) vs Orillusion (WebGPU)
> **테스트 모델**: `free_cyberpunk_hovercar.glb` (~27MB, 126 메시)
> **환경**: LG Gram → LAN → Ubuntu Docker 서버 (192.168.0.16)
> **브라우저**: Chrome 시크릿 모드 + 강력 새로고침 (캐시 완전 제거)

---

## 목차

- [원본 측정 데이터](#원본-측정-데이터)
  - [측정 기준](#측정-기준)
  - [Raw Data](#raw-data-단위-ms)
  - [엔드포인트 구성](#엔드포인트-구성)
- [1. Widget 분리가 확실히 효과가 있는가?](#1-widget-분리가-확실히-효과가-있는가)
  - [결론](#결론-극적인-차이는-없다-하지만-분리가-손해를-보지도-않는다)
  - [왜 차이가 작은가?](#왜-차이가-작은가)
  - [Three.js 1회차 이상치 분석](#threejs-분리-1회차3506ms는-왜-튀었나)
  - [Widget 분리의 장점](#widget-분리web-host의-장점)
    - [협업 개발 — Web팀과 3D팀 분리](#가장-큰-장점-협업-개발-시-web팀과-3d팀의-완전-분리)
    - [헥사고날 아키텍처 유사성](#헥사고날-아키텍처와의-유사성--인터페이스-기반-테스트-용이성)
  - [통합이 유리한 경우](#통합combined이-유리한-경우)
- [2. 어느 엔진이 더 빠른가? 왜?](#2-어느-엔진이-더-빠른가-왜)
  - [순위](#순위-초기-로딩-시간-기준-평균)
  - [Three.js가 가장 빠른 이유](#threejs가-가장-빠른-이유)
  - [Orillusion이 느린 이유](#orillusion이-느린-이유-3가지-겹침)
  - [WebGPU가 빛나는 영역](#하지만-webgpu가-빛나는-영역은-다르다)
- [3. 로딩 속도 편차가 클 때는 무엇 때문인가?](#3-로딩-속도-편차가-클-때는-무엇-때문인가)
  - [현상 — 실측 편차](#현상--실측-데이터에서-확인되는-편차)
  - [원인 순위](#원인-순위)
  - [빠른 진단법](#빠른-진단법)
  - [편차 줄이는 법](#편차-줄이는-법)
- [4. 프로젝트 성향별 엔진 선택 가이드](#4-프로젝트-성향별-엔진-선택-가이드)
  - [선택 기준 표](#한눈에-보는-선택-기준)
  - [엔진별 상세 비교](#엔진별-상세-비교)
  - [의사결정 플로우차트](#의사결정-플로우차트)
- [5. WebGPU 테스트 — Orillusion 사용기](#5-webgpu-테스트--orillusion-사용기)
  - [장점](#장점)
  - [단점](#단점)
  - [실제 겪은 삽질 요약](#실제-겪은-삽질-요약)
  - [Orillusion을 써야 하는가?](#orillusion을-써야-하는가)
- [6. WebLLM 사용기](#6-webllm-사용기)
- [부록: 네트워크 대역폭별 전송 시간 추정](#부록-네트워크-대역폭별-27mb-glb-전송-시간-추정)

---

## 원본 측정 데이터

### 측정 기준

```
시작: window.__PAGE_START = performance.now()  (HTML <script>에서 기록)
종료: mount().ready Promise 해결 시점
  = GLB 로드 완료
  + scene.whenReadyAsync() (Babylon) / 렌더러 준비 (Three.js) / FPS 안정화 (Orillusion)
  + waitFrames(scene, 8) (8프레임 렌더 안정화)
표시: performance.now() - __PAGE_START (ms)
조건: Chrome 시크릿 모드 + 강력 새로고침 (Ctrl+Shift+R), 3회 반복
```

### Raw Data (단위: ms)

| 장비 | 엔드포인트 | 1회차 | 2회차 | 3회차 | **평균** |
|------|-----------|------:|------:|------:|--------:|
| 그램_1 | 5100 (webHost_babylon) | 3,385 | 3,551 | 3,738 | **3,558** |
| 그램_1 | 5102 (combine_babylon) | 3,489 | 3,526 | 3,833 | **3,616** |
| 그램_1 | 5200 (webHost_three) | 3,506 | 2,293 | 2,247 | **2,682** |
| 그램_1 | 5202 (combine_three) | 2,662 | 2,345 | 2,101 | **2,369** |
| 그램_1 | 5300 (orillusion) | 6,828 | 7,673 | 7,047 | **7,183** |

### 엔드포인트 구성

| 포트 | 엔진 | 모드 | 설명 |
|------|------|------|------|
| **5100** | Babylon.js (WebGL) | web-host (분리) | 호스트 앱이 :5101의 widget embed.js를 런타임 `import()` |
| **5101** | Babylon.js (WebGL) | 3d-widget | 3D 위젯 서버 (embed.js + GLB 에셋 제공) |
| **5102** | Babylon.js (WebGL) | combined (통합) | 위젯 코드를 정적 import로 한 번들에 포함 |
| **5200** | Three.js (WebGL) | web-host (분리) | 호스트 앱이 :5201의 widget embed.js를 런타임 `import()` |
| **5201** | Three.js (WebGL) | 3d-widget | 3D 위젯 서버 |
| **5202** | Three.js (WebGL) | combined (통합) | 위젯 코드를 정적 import로 한 번들에 포함 |
| **5300** | Orillusion (WebGPU) | 단일 앱 | Vite dev 서버 단독 실행 |

> **web-host vs combined**: 둘 다 iframe을 사용하지 않는다. 동일 JS 컨텍스트에서 실행.
> 차이는 위젯 코드를 **런타임 cross-origin import** (분리)하느냐, **정적 import로 같은 번들** (통합)에 넣느냐.

---

## 1. Widget 분리가 확실히 효과가 있는가?

### 결론: "극적인 차이는 없다. 하지만 분리가 손해를 보지도 않는다."

| 엔진 | web-host (분리) | combined (통합) | 차이 | 더 빠른 쪽 |
|------|----------------:|----------------:|-----:|-----------|
| Babylon.js | 3,558ms | 3,616ms | 58ms | **분리** |
| Three.js | 2,682ms | 2,369ms | 313ms | **통합** |

#### 수치로 보면

- **Babylon.js**: 분리(3,558ms)가 통합(3,616ms)보다 **58ms 빠름**
  - 3회 측정 편차: 분리 3,385~3,738 / 통합 3,489~3,833
  - 58ms 차이는 측정 편차(353ms, 344ms) 안에 들어가므로 **통계적으로 유의미하지 않음**
  - 다만, 3회 모두 분리가 통합보다 빨랐음 (3,385<3,489 / 3,551>3,526 / 3,738<3,833) → 2승 1패

- **Three.js**: 통합(2,369ms)이 분리(2,682ms)보다 **313ms 빠름**
  - 3회 측정 편차: 분리 2,247~3,506 / 통합 2,101~2,662
  - **분리 1회차(3,506ms)가 이상치** — 2·3회차(2,293, 2,247)는 통합과 거의 동등
  - 1회차 제외 시 분리 평균 = 2,270ms → 통합(2,369ms)과 **거의 동일하거나 오히려 빠름**

### 왜 차이가 작은가?

**병목이 27MB GLB 전송 (전체의 40~60%)**이기 때문이다.

```
embed.js fetch 오버헤드 = ~50-150ms = 전체의 3~5%
27MB GLB 전송           = ~500-2500ms = 전체의 40~60%
```

embed.js를 분리하든 통합하든, 진짜 병목인 GLB 전송 시간은 동일하다.

### Three.js 분리 1회차(3,506ms)는 왜 튀었나?

Three.js web-host 1회차만 3,506ms로 나머지(2,293, 2,247)와 **1,200ms 이상 차이**가 난다.
이상치를 제외하면 분리 평균 = 2,270ms → 통합(2,369ms)보다 **오히려 99ms 빠르다**.
즉, **이론대로 web-host가 빠르거나 최소 동등한 것이 맞다**. 1회차 이상치 하나가 평균을 왜곡한 것.

#### 가능한 원인 3가지

**1) Wi-Fi 순간 대역폭 저하**

> **뜻**: 무선 네트워크는 같은 자리에서도 순간적으로 속도가 떨어질 수 있다. 27MB GLB를 매번 풀 다운로드하므로 Wi-Fi 상태가 로딩 시간을 직접 결정한다.

```
평소:  Wi-Fi 300Mbps → GLB ~0.7초
1회차: Wi-Fi  80Mbps → GLB ~2.7초  ← 이것만으로 2초 차이
```

간섭 원인: 옆집 공유기, 같은 네트워크의 다른 기기, 블루투스(2.4GHz), 사람 이동, 전자레인지 등

**확인 방법**:
```
Chrome DevTools → Network 탭 → free_cyberpunk_hovercar.glb 클릭
→ Timing 탭에서 "Content Download" 시간 확인
→ 평소 800ms인데 3000ms+ 이면 Wi-Fi 문제
```

**2) Docker 콜드 스타트 (Vite 프로세스 웜업)**

> **뜻**: Docker 컨테이너 안의 Vite dev 서버가 처음 요청을 받으면, Node.js 프로세스가 "차가운 상태"에서 시작한다. 모듈 그래프 구성, 의존성 변환 등을 처음에만 수행하므로 첫 요청이 이후 요청보다 느리다. 2회차부터는 이미 웜업된 상태라 빨라진다.

```
1회차: Vite가 embed.js + 의존성 모듈 그래프를 처음 구성 → 느림
2회차: 이미 구성된 모듈 그래프 재사용 → 빠름
```

**확인 방법**:
```
# Docker 컨테이너의 Vite 로그에서 첫 요청 처리 시간 확인
docker logs <container-name> 2>&1 | grep "request"

# 또는: 서버 측에서 첫 요청 vs 두 번째 요청의 응답 시간 비교
# 첫 요청만 유독 느리고 이후 일정하면 → 콜드 스타트 확정
```

**3) 브라우저 첫 시크릿 윈도우 (V8 JIT 웜업)**

> **뜻**: Chrome이 시크릿 윈도우를 새로 열면 V8 JavaScript 엔진이 "차가운 상태"로 시작한다. 코드를 처음 실행할 때는 인터프리터로 느리게 실행하고, 반복 실행되는 코드만 JIT(Just-In-Time) 컴파일러가 기계어로 최적화한다. 첫 페이지 로드 시에는 이 최적화가 아직 안 되어있어 JS 파싱+실행이 느리다.

```
1회차: V8 인터프리터 모드 (느림) + JIT 컴파일 비용
2회차: JIT 최적화 코드 재활용 (빠름) — 시크릿 모드라도 같은 탭에선 유지
```

**확인 방법**:
```
Chrome DevTools → Performance 탭 → 녹화 시작 → 페이지 로드
→ "Scripting" (노란색) 영역의 비율 확인
→ 1회차에서 Scripting이 전체의 40%+ 차지하고, 2회차에서 20% 이하면
→ V8 JIT 웜업 영향 확정
```

#### 결론

> **이론대로 web-host(분리)가 combined(통합)보다 빠르거나 동등한 것이 맞다.**
> Three.js 평균에서 통합이 빨라 "보이는" 이유는 1회차 이상치(3,506ms) 하나가 원인이며,
> 이는 성능 차이가 아니라 **Wi-Fi / 콜드 스타트 / JIT 웜업** 같은 외부 요인이다.

### Widget 분리(web-host)의 장점

#### 가장 큰 장점: 협업 개발 시 Web팀과 3D팀의 완전 분리

```
┌─────────────────────┐          ┌─────────────────────┐
│   Web 개발팀         │          │   3D 개발팀          │
│                     │          │                     │
│  apps/web-host/     │          │  apps/3d-widget/    │
│  ├─ UI/UX           │   약속   │  ├─ Babylon/Three   │
│  ├─ 상태 관리        │◄────────►│  ├─ GLB 로드        │
│  ├─ API 연동         │ embed.js │  ├─ 셰이더/재질     │
│  └─ 비즈니스 로직    │  인터페이스│  └─ 애니메이션      │
│                     │          │                     │
│  독자 배포 ✓         │          │  독자 배포 ✓         │
│  독자 테스트 ✓       │          │  독자 테스트 ✓       │
│  독자 스택 선택 ✓    │          │  독자 스택 선택 ✓    │
└─────────────────────┘          └─────────────────────┘
```

- **서로의 코드를 몰라도 된다** — Web팀은 `mount(canvas)` / `dispose()`만 알면 되고, 3D팀은 canvas 엘리먼트만 받으면 됨
- **서로 다른 릴리즈 주기** — 3D 위젯만 업데이트하거나, 호스트 UI만 업데이트할 수 있음
- **Git 충돌 최소화** — 별도 레포 또는 별도 디렉토리로 관리 → 같은 파일 수정 충돌 없음
- **기술 스택 독립** — Web팀은 React/Vue/Svelte 중 자유롭게, 3D팀은 엔진 교체 가능 (인터페이스만 유지)

#### 헥사고날 아키텍처와의 유사성 — 인터페이스 기반 테스트 용이성

이 분리 구조는 **헥사고날 아키텍처(Ports & Adapters)**의 핵심 원리를 공유한다:
양쪽이 **구현이 아닌 인터페이스(Port)를 통해서만 통신**하므로, 상대방을 몰라도 독립적으로 테스트할 수 있다.

```
┌──────────────────────────────────────────────────────────┐
│  헥사고날 아키텍처 원리                                     │
│                                                          │
│  외부 시스템 ──► [Port(인터페이스)] ──► 핵심 로직          │
│                  ↑ 이 경계만 맞추면 양쪽 독립              │
│                                                          │
│  Widget 분리에 적용하면                                    │
│                                                          │
│  Web Host ──► [embed.js 인터페이스] ──► 3D Widget         │
│               mount(canvas) → controller                 │
│               controller.dispose()                       │
│               ↑ 이 약속만 지키면 양쪽 독립                 │
└──────────────────────────────────────────────────────────┘
```

**인터페이스(Port)가 되는 것**: `embed.js`가 export하는 `mount(canvas)` 함수와 반환하는 `controller` 객체

| 헥사고날 아키텍처 | Widget 분리 | 공통 원리 |
|-----------------|------------|----------|
| Port (인터페이스) | `mount(canvas) → { dispose() }` | 양쪽이 합의한 계약 |
| Adapter (어댑터) | embed.js (3D 엔진을 감싸서 인터페이스로 노출) | 구현을 인터페이스로 변환 |
| 핵심 로직 | Web Host 비즈니스 로직 / 3D 렌더링 로직 | 각자의 관심사에만 집중 |
| 의존성 방향 | 외부 → Port → 내부 | Host → embed.js 인터페이스 → 3D 엔진 |

**테스트가 쉬워지는 이유**: 인터페이스를 통해서만 값이 넘어오므로, 상대방을 **Mock**으로 대체할 수 있다.

```typescript
// Web팀: 3D 위젯 없이 호스트만 테스트
const mockWidget = {
  mount(canvas: HTMLCanvasElement) {
    return {
      ready: Promise.resolve(),
      dispose() { /* no-op */ }
    }
  }
}
// → 3D 엔진 없이 UI 로직, 상태 관리, API 연동 테스트 가능

// 3D팀: 호스트 없이 위젯만 테스트
const testCanvas = document.createElement('canvas')
const controller = mount(testCanvas)
await controller.ready
// → 호스트 UI 없이 렌더링, 모델 로딩, 애니메이션 테스트 가능
```

> 정확히 헥사고날 아키텍처 그 자체는 아니지만(도메인 모델 중심 설계는 아니므로),
> **"인터페이스 경계를 통해서만 통신하고, 양쪽을 독립적으로 개발/테스트한다"**는 핵심 원리를 동일하게 적용한 구조다.

#### 성능 측면

| 장점 | 설명 |
|------|------|
| **번들 분할 → 병렬 로딩** | 호스트 JS가 작아 빠르게 파싱 완료 → embed.js fetch가 병렬로 진행. Babylon.js에서 실측으로 확인 (분리가 58ms 빠름) |
| **CDN 엣지 캐시** | 프로덕션에서 widget을 CDN에 배포하면 사용자에게 가장 가까운 서버에서 서빙 |
| **크로스 서비스 캐시 재사용** | 여러 서비스가 같은 위젯 URL을 쓰면 브라우저가 캐시 공유 → 재방문 시 네트워크 요청 없음 |
| **HTTP/2 멀티플렉싱** | 여러 작은 리소스를 병렬 전송할 때 단일 큰 번들보다 유리할 수 있음 |

#### 운영/아키텍처 측면

| 장점 | 설명 |
|------|------|
| **독립 배포/롤백** | 위젯에 버그가 있으면 위젯만 롤백 — 호스트는 영향 없음 |
| **버전 관리** | `/v1/embed.js`, `/v2/embed.js`로 여러 버전 동시 운영 가능 |
| **장애 격리** | 위젯 서버 장애 시 호스트는 정상 작동 (폴백 UX 제공 가능) |
| **A/B 테스트** | 위젯 URL만 바꿔서 다른 버전의 3D 렌더러를 테스트 가능 |

### 통합(combined)이 유리한 경우

| 조건 | 이유 |
|------|------|
| **1인 개발 / 소규모 팀** | 분리의 협업 이점이 없고, 복잡도만 증가 |
| **번들이 작은 엔진** (Three.js) | 통합 번들의 불이익이 적고, embed.js fetch RTT 제거 효과가 더 큼 |
| **운영 단순화** | CORS/CSP 설정 불필요, 서버 1대로 충분 |
| **단일 서비스** | 재사용 이점이 없으므로 분리는 오버엔지니어링 |

### 핵심

> **Widget 분리의 가치는 "로딩 속도"가 아니라 "협업과 아키텍처"에 있다.**
> - 로딩 속도: 분리 ≈ 통합 (전체의 3~5% 차이로 무의미)
> - 협업: **Web팀과 3D팀을 완전히 분리해서 독자적으로 개발/배포 가능** — 이것이 분리의 핵심 가치
> - 운영: 독립 배포, CDN 캐시, 버전 관리, 장애 격리 등 프로덕션 운영에 유리
> - 단, 1인 개발이나 단일 서비스면 통합이 더 합리적

---

## 2. 어느 엔진이 더 빠른가? 왜?

### 순위 (초기 로딩 시간 기준, 평균)

```
1위  Three.js combined     2,369ms  ████████████
2위  Three.js web-host     2,682ms  █████████████▌
3위  Babylon.js web-host   3,558ms  █████████████████▊
4위  Babylon.js combined   3,616ms  ██████████████████
5위  Orillusion (WebGPU)   7,183ms  ████████████████████████████████████
```

### 엔진 단위 비교 (최선 모드 기준)

| 엔진 | 최선 모드 | 평균 | 최소값 | 최대값 | Babylon 대비 |
|------|----------|-----:|-------:|-------:|:------------|
| **Three.js** | combined | **2,369ms** | 2,101 | 2,662 | **1.53배 빠름** |
| **Babylon.js** | web-host | **3,558ms** | 3,385 | 3,738 | 기준 |
| **Orillusion** | 단일 앱 | **7,183ms** | 6,828 | 7,673 | **2.02배 느림** |

### Three.js가 가장 빠른 이유

| 요인 | Three.js | Babylon.js |
|------|----------|-----------|
| **코어 번들 크기** | 작음 (~600KB gzip) | 큼 (~1MB+ gzip) |
| **초기화 방식** | 가볍고 즉시적 | 더 많은 내부 시스템 초기화 |
| **GLB 파서** | 경량 (GLTFLoader) | 풍부하지만 무거움 (SceneLoader) |
| **Tree-shaking** | 효과적 (ESM 설계) | 상대적으로 제한적 |

Three.js는 "필요한 것만 가져오는" 철학으로, 단일 모델 로딩 시나리오에서 오버헤드가 가장 적다.

### Orillusion이 느린 이유 (3가지 겹침)

**1) WebGPU 초기화 오버헤드 (본질적)**

| 단계 | WebGL | WebGPU |
|------|-------|--------|
| 컨텍스트 | `getContext('webgl')` 동기 | `requestAdapter()` + `requestDevice()` 비동기 |
| 셰이더 | 첫 drawcall 시 암묵적 | 파이프라인 생성 시 **사전 컴파일** (WGSL→바이너리) |
| 파이프라인 | GL state machine | `createRenderPipeline()` 명시적 |

WebGPU는 **런타임 jank 제거를 위해 초기화 비용을 앞당긴** 설계다. 대신 렌더링 중 프레임 드롭이 적다.

**2) 엔진 미성숙 (v0.8.5-dev.9 알파)**

- GLB 파서 버그로 **수동 패치 필요** (`glbNodeAnimation.ts`에서 직접 GLB 바이너리 파싱)
- 비골격 노드 애니메이션 미지원 → 커스텀 런타임으로 보완
- `GLTF_NODE_INDEX_PROPERTY`가 `undefined`로 선언되어 `obj['undefined']`에 nodeId 저장하는 버그

**3) 테스트 조건 불이익**

- **비압축 소스** 사용 (~2.16MB `orillusion.es.max.js`) — Vite prebundling 버그 우회
- **추가 작업**: HDR 큐브맵 + SkyRenderer + Bloom + TAA + 커스텀 노드 애니메이션 파서 (GLB 이중 fetch)

### Orillusion 7,183ms 분해 추정

```
┌───────────────────────────────────────────────────────────┐
│  2.16MB JS 전송+파싱               ~300-500ms             │
│  WebGPU 초기화 (adapter+device)    ~200-400ms             │
│  HDR 큐브맵 로드+SkyRenderer       ~200-400ms  ← 추가 작업│
│  GLB 27MB 전송 (LAN)              ~500-2500ms ← 최대 병목 │
│  GLB 파싱 + GPU 파이프라인 생성    ~500-1000ms             │
│  Bloom+TAA 포스트프로세싱 설정     ~100-200ms  ← 추가 작업│
│  커스텀 노드 애니메이션 파서       ~200-500ms  ← GLB 이중 fetch│
│  WGSL 셰이더 사전 컴파일          ~300-600ms             │
│  30프레임 FPS 안정화 대기          ~500-1000ms             │
│                                                           │
│  총합: ~2,800ms ~ 7,100ms                                 │
│  실측 평균: 7,183ms                                        │
└───────────────────────────────────────────────────────────┘
```

### 하지만 WebGPU가 빛나는 영역은 다르다

| 시나리오 | WebGL | WebGPU |
|---------|-------|--------|
| 단일 모델 로딩 속도 | **빠름** | 느림 |
| 100+ 오브젝트 렌더링 FPS | CPU 바운드 시작 | 여유 있음 |
| 1000+ 드로우콜 | **CPU 병목 심각** | CPU 오버헤드 대폭 감소 |
| GPU Compute | 불가 | **네이티브 지원** |
| 파티클 100만 개 | CPU 한계 | Compute Shader로 처리 |

> 이번 테스트는 "GLB 1개 로드 → 안정화 시간"을 측정했으므로 **WebGPU의 장점이 전혀 드러나지 않는 영역**이다.

---

## 3. 로딩 속도 편차가 클 때는 무엇 때문인가?

### 현상 — 실측 데이터에서 확인되는 편차

| 엔드포인트 | 최소 | 최대 | **편차 (max-min)** | 편차율 |
|-----------|-----:|-----:|-------------------:|-------:|
| 5100 (webHost_babylon) | 3,385 | 3,738 | 353ms | 10% |
| 5102 (combine_babylon) | 3,489 | 3,833 | 344ms | 10% |
| 5200 (webHost_three) | 2,247 | **3,506** | **1,259ms** | **47%** |
| 5202 (combine_three) | 2,101 | 2,662 | 561ms | 24% |
| 5300 (orillusion) | 6,828 | 7,673 | 845ms | 12% |

**Three.js web-host의 1회차(3,506ms)**가 눈에 띄게 튀었고, 동일 조건에서도 **3초 vs 8초** 수준의 편차가 관찰된 적이 있다.

### 원인 순위

#### 1순위: Wi-Fi 대역폭 변동 (가장 유력)

매번 27MB를 네트워크로 풀 다운로드하므로 Wi-Fi 상태가 로딩 시간을 **직접 결정**한다.

```
평소:  Wi-Fi 300Mbps → GLB ~0.7초 → 총 ~3초
가끔:  Wi-Fi  50Mbps → GLB ~4.3초 → 총 ~8초
                        ↑
                    이 차이만으로 5초 벌어짐
```

Wi-Fi 간섭 원인:
- 같은 채널의 옆집 공유기
- 같은 네트워크의 다른 기기 대용량 다운로드
- 블루투스 간섭 (2.4GHz)
- 사람 이동에 의한 순간 신호 감쇠
- 전자레인지 등 전자파

#### 2순위: LG Gram CPU 서멀 스로틀링

```
정상: CPU 4.0GHz → JS 파싱 + GLB 디코딩 빠름 → 3초
과열: CPU 1.8GHz → 처리 속도 절반             → 6~8초
```

얇은 노트북이라 직전 작업에 따라 CPU 클럭이 크게 변동.

#### 3순위: Docker 컨테이너 리소스 경합

4개 컨테이너가 같은 CPU/메모리/디스크 I/O를 공유.
다른 컨테이너가 동시에 요청을 처리하거나 Node.js GC가 돌면 디스크 I/O / CPU 경합 발생.

#### 4순위: V8 GC (상대적으로 영향 적음)

27MB ArrayBuffer 파싱 시 수백MB 임시 객체 → Major GC 시 수백ms 멈춤 가능.
다만 시크릿 모드는 매번 깨끗한 힙에서 시작하므로 영향은 상대적으로 적다.

### 빠른 진단법

Network 탭에서 `free_cyberpunk_hovercar.glb`의 Time만 비교:
- **GLB Time이 길면** → 네트워크 문제
- **GLB Time은 비슷한데 전체가 느리면** → CPU/GC 문제

### 편차 줄이는 법

| 방법 | 효과 | 난이도 |
|------|------|-------|
| **유선 LAN으로 테스트** | Wi-Fi 변동 완전 제거 (가장 확실) | 낮음 |
| GLB gzip/brotli 압축 서빙 | 27MB → ~5-8MB 전송량, 네트워크 영향 감소 | 중간 |
| 테스트 전 노트북 5분 쿨다운 | CPU 서멀 스로틀링 방지 | 낮음 |
| 불필요한 Docker 컨테이너 중지 | 서버 리소스 경합 감소 | 낮음 |
| 5회 이상 측정, 최고/최저 제외 평균 | 통계적 신뢰도 향상 | 낮음 |

---

## 4. 프로젝트 성향별 엔진 선택 가이드

### 한눈에 보는 선택 기준

| 프로젝트 성향 | 추천 엔진 | 이유 |
|-------------|----------|------|
| **제품 뷰어 / 쇼케이스** (모델 1~5개) | **Three.js** | 가장 빠른 초기 로딩 (2,369ms), 가벼운 번들, 풍부한 생태계 |
| **인터랙티브 웹앱** (중간 복잡도) | **Three.js** | React Three Fiber 등 프레임워크 통합 우수 |
| **대규모 씬** (100+ 오브젝트, 복잡한 포스트프로세싱) | **Babylon.js** | 빌트인 물리엔진, GUI, Inspector, 프로덕션 검증 |
| **게임 / 시뮬레이션** (물리, 충돌, AI) | **Babylon.js** | 게임 엔진 수준의 통합 기능 |
| **대량 렌더링** (1000+ 드로우콜, 파티클 시뮬) | **Orillusion (WebGPU)** | GPU Compute, CPU 오버헤드 최소, 미래 지향 |
| **GPU Compute 활용** (ML 추론, 물리 시뮬) | **Orillusion (WebGPU)** | Compute Shader 네이티브 |
| **위젯 / 임베드** (외부 사이트에 삽입) | **Three.js** | 번들 크기 최소, tree-shaking 효과적 |
| **빠른 프로토타이핑** | **Three.js** | 낮은 러닝커브, 방대한 예제/커뮤니티 |
| **엔터프라이즈 / 장기 프로젝트** | **Babylon.js** | Microsoft 지원, 안정적 LTS, 문서 품질 |

### 엔진별 상세 비교

| 항목 | Three.js | Babylon.js | Orillusion |
|------|----------|-----------|-----------|
| **렌더링 API** | WebGL (2.0) | WebGL (2.0) | WebGPU |
| **버전** | r160+ (안정) | 7.x (안정) | 0.8.5-dev.9 (알파) |
| **개발 기간** | ~12년 | ~10년 | ~3년 |
| **번들 크기** | ~600KB gzip | ~1MB+ gzip | 2.16MB 비압축 |
| **초기 로딩 (실측)** | **2,369ms** | 3,558ms | 7,183ms |
| **GLB 파서** | 프로덕션 검증 | 프로덕션 검증 | 버그 있음 (수동 패치) |
| **Tree-shaking** | 효과적 | 제한적 | N/A (비압축 소스) |
| **커뮤니티** | 최대 | 크고 활발 | 매우 작음 |
| **문서** | 풍부 | 풍부 | 빈약 |
| **GPU Compute** | 불가 | 불가 | 네이티브 |
| **지원** | 오픈소스 커뮤니티 | Microsoft | 소규모 팀 |

### 의사결정 플로우차트

```
프로젝트에 GPU Compute가 필요한가?
├─ Yes → Orillusion (WebGPU) — 단, 프로덕션 안정성은 감수
│
├─ No → 오브젝트 수가 100개 이상인가?
│       ├─ Yes → 물리/충돌/GUI가 필요한가?
│       │       ├─ Yes → Babylon.js
│       │       └─ No  → Three.js (인스턴싱 활용) 또는 Babylon.js
│       │
│       └─ No → 번들 크기/로딩 속도가 최우선인가?
│               ├─ Yes → Three.js
│               └─ No  → Three.js (범용) 또는 Babylon.js (통합 기능)
```

---

## 5. WebGPU 테스트 — Orillusion 사용기

### 장점

| 항목 | 설명 |
|------|------|
| **WebGPU 네이티브** | WebGL 래핑이 아닌 처음부터 WebGPU 전용 설계. GPU 파이프라인/셰이더를 직접 제어 |
| **Compute Shader 지원** | 파티클, 물리 시뮬레이션 등을 GPU에서 직접 처리 가능 |
| **포스트 프로세싱 내장** | Bloom, TAA 등을 엔진 레벨에서 바로 적용 가능. 설정도 비교적 간단 |
| **컴포넌트 기반 아키텍처** | Unity 스타일의 `addComponent()` 패턴. 익숙하면 직관적 |
| **HDR 환경맵 로드** | `loadHDRTextureCube()` 한 줄로 HDR 큐브맵 적용. SkyRenderer도 간단 |
| **TypeScript 퍼스트** | 소스가 TS로 작성되어 타입 지원이 기본 |
| **렌더링 중 프레임 안정성** | WebGPU 특성상 셰이더 사전 컴파일로 렌더링 중 jank가 적음 |

### 단점

| 항목 | 설명 | 심각도 |
|------|------|--------|
| **GLB 파서 버그** | 비골격(노드) 애니메이션을 처리하지 못함. `GLTF_NODE_INDEX_PROPERTY`가 `undefined`로 선언되어 `obj['undefined']`에 nodeId를 저장하는 버그. 결국 GLB를 직접 바이너리 파싱하는 커스텀 런타임(`glbNodeAnimation.ts`)을 작성해야 했음 | **높음** |
| **Vite prebundling 충돌** | npm 패키지를 정상적으로 import하면 Vite가 prebundle 시 에러 발생. 비압축 소스(`orillusion.es.max.js` 2.16MB)를 `src/lib/`에 수동 복사해서 우회해야 했음 | **높음** |
| **문서/예제 부족** | 공식 문서가 빈약하고, 에러 발생 시 소스 코드를 직접 읽어야 함. 커뮤니티가 작아서 Stack Overflow 등에서 답을 찾기 어려움 | **높음** |
| **알파 버전 (v0.8.5-dev.9)** | API가 안정되지 않음. 메이저 업데이트 시 breaking change 가능성 높음 | **중간** |
| **초기 로딩 느림** | WebGPU 특성 + 엔진 미최적화로 초기 로딩이 WebGL 대비 2~3배 느림 (7,183ms vs 2,369ms) | **중간** |
| **브라우저 호환성** | WebGPU를 지원하는 브라우저에서만 실행 가능 (Chrome 113+, Edge 113+, Firefox는 실험적) | **중간** |
| **TypeScript 타입 불일치** | 엔진 내부 타입과 실제 동작이 다른 경우가 있어 `as any` 캐스팅이 빈번하게 필요 | **낮음** |

### 실제 겪은 삽질 요약

1. **GLB 애니메이션 재생 실패** → Orillusion의 GLB 파서가 노드 애니메이션을 무시 → 직접 GLB 바이너리를 파싱해서 `translation/rotation/scale` 채널을 추출, 매 프레임 transform 갱신하는 커스텀 컴포넌트 작성
2. **노드 탐색 실패** → `GLTF_NODE_INDEX_PROPERTY`가 `undefined`라서 모든 노드가 `obj['undefined']` 키에 저장됨 → 노드 이름 기반 탐색으로 전환
3. **회전 적용 안됨** → `localRotQuat`에 인라인 `set()`을 써도 dirty 플래그가 안 세워짐 → setter 할당 방식으로 변경
4. **Vite에서 import 실패** → npm 패키지 그대로 쓰면 prebundling 에러 → 소스 파일을 프로젝트 내부에 복사
5. **TAAPost/BloomPost 타입 에러** → `addPost()`/`removePost()`가 인스턴스가 아닌 클래스를 받는데 TS 타입이 안 맞음 → `as any` 처리

### Orillusion을 써야 하는가?

| 상황 | 판단 |
|------|------|
| WebGPU의 Compute Shader가 반드시 필요한 R&D/프로토타입 | **써볼 만하다** |
| 대량 렌더링 성능 실험 | **써볼 만하다** |
| 프로덕션 서비스 | **아직 이르다** — GLB 파서 버그, API 불안정, 문서 부족 |
| WebGPU 학습 목적 | **괜찮다** — WebGPU 파이프라인 구조를 이해하기에는 나쁘지 않음 |

---

## 6. WebLLM 사용기

[WebLLM](https://webllm.mlc.ai/)은 **브라우저 안에서 LLM을 직접 실행**하는 추론 엔진이다.
서버 없이 WebGPU로 GPU 가속 추론을 수행한다.

상세 내용은 별도 문서 참고: [`webllm/docs/webllm-overview.md`](../webllm/docs/webllm-overview.md)

> TODO: 직접 테스트 후 사용기 추가 예정

---

## 부록: 네트워크 대역폭별 27MB GLB 전송 시간 추정

| 네트워크 | 대역폭 | 27MB 전송 시간 | 비고 |
|---------|-------|---------------|------|
| localhost | 메모리/디스크 속도 | ~0ms | 네트워크 없음 |
| LAN 유선 (1Gbps) | ~125 MB/s | ~0.2초 | |
| LAN Wi-Fi (300Mbps) | ~37 MB/s | ~0.7초 | 실제 환경에 가까움 |
| LAN Wi-Fi (100Mbps) | ~12 MB/s | ~2.2초 | 혼잡 시 |
| 인터넷 (50Mbps) | ~6 MB/s | ~4.5초 | 일반 가정 |
| 모바일 (10Mbps) | ~1.2 MB/s | ~22초 | 4G LTE |
